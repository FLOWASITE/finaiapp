Thay text nút "Tạo HĐ bán" → "Phiếu bán hàng" trên màn hình Bán hàng (`src/routes/_app/sales/index.tsx`).

- Cập nhật `label` trong cấu hình action button (dòng ~196).
- Cập nhật text trực tiếp trong JSX (dòng ~1566).